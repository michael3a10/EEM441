% !TEX program = lualatex
% !TeX program = lualatex
% !TEX TS-program = lualatex
% Note: this document MUST be compiled with LuaLaTeX (not pdfLaTeX/XeLaTeX)
% because it uses fontspec/unicode-math for custom fonts. No -shell-escape
% is required -- code highlighting uses the listings package, not minted.
% Compile 3x for the table of contents and cross-references to resolve.
\documentclass[12pt]{article}
\input{preamble.tex}

\title{EEM 441 --- Control, Instrumentation \& FPGA Laboratory}
\author{}
\date{}

\begin{document}

% ---------- Cover page ----------
\begin{titlepage}
    \centering
    \vspace*{2.2cm}
    \includegraphics[width=0.5\textwidth]{figures/usm-logo.png}\par
    \vspace{2.0cm}
    {\plexlight\color{slategray}\Large Universiti Sains Malaysia\par}
    \vspace{0.3em}
    {\plexlight\color{slategray}\large School of Electrical and Electronic Engineering\par}
    \vspace{2.2cm}
    {\plexmedium\color{usmpurple}\fontsize{34}{40}\selectfont EEM 441\par}
    \vspace{0.5em}
    {\plexmedium\color{inkblack}\Large Control, Instrumentation \& FPGA Laboratory\par}
    \vspace{0.6em}
    {\plexlight\color{slategray}\large Laboratory Manual\par}
    \vfill
    {\plexlight\color{slategray}\normalsize Experiments 1--10\par}
    \vspace{2cm}
\end{titlepage}

% ---------- Table of contents ----------
\pagestyle{empty}
\tableofcontents
\clearpage
\pagestyle{fancy}
\setcounter{page}{1}

\input{experiments/exp01.tex}
\input{experiments/exp02.tex}
\input{experiments/exp03.tex}
\input{experiments/exp04.tex}
\input{experiments/exp05.tex}
\input{experiments/exp06.tex}
\input{experiments/exp07.tex}
\input{experiments/exp08.tex}
\input{experiments/exp09.tex}
\input{experiments/exp10.tex}

\end{document}


% ============================================================
% VS Code / LaTeX Workshop setup (one-time, per machine)
% ============================================================
% This document only needs plain LuaLaTeX -- no -shell-escape, no
% external programs. (Code highlighting uses the listings package,
% not minted, so there's no Pygments/latexminted dependency either.)
%
% This project already includes a .vscode/settings.json with a
% "lualatex x3" recipe (three plain LuaLaTeX passes, needed so the
% table of contents and cross-references resolve). To use it:
%   1. In VS Code: File -> Open Folder... and select the folder that
%      contains this main.tex (not a parent folder, not just the file).
%   2. Open main.tex from the Explorer sidebar.
%   3. Press Ctrl+Alt+B (or the green play button).
%
% If you'd rather configure this by hand instead of relying on the
% bundled .vscode/settings.json, add the following to your own
% settings.json (User or Workspace settings -> "Open Settings (JSON)"):
%
% {
%   "latex-workshop.latex.tools": [
%     {
%       "name": "lualatex-plain",
%       "command": "lualatex",
%       "args": [
%         "-synctex=1",
%         "-interaction=nonstopmode",
%         "-file-line-error",
%         "%DOC%"
%       ]
%     }
%   ],
%   "latex-workshop.latex.recipes": [
%     {
%       "name": "lualatex x3",
%       "tools": ["lualatex-plain", "lualatex-plain", "lualatex-plain"]
%     }
%   ],
%   "latex-workshop.latex.recipe.default": "lualatex x3"
% }
%
% Make sure preamble.tex, the experiments/ folder, and the fonts/
% folder are all sitting next to this main.tex file -- they are
% loaded via relative paths (\input{preamble.tex}, fonts/PlexSans/
% etc.) and will fail to be found otherwise.
